- Support multi-valued switches in the command line parser.

- Support verbs with different argument lists.

- Specify whether unnamed arguments are supported and fail if encountered.

- Mandatory and optional switches (per verb).

- Allow core usage string to be customised, e.g. test runner filters.
