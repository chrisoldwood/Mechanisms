- Support multi-valued switches in the command line parser.

- Report better messages for command line parsing errors.

- Add command line parser method for building the usage message switch list.

- Support verbs with different argument lists.

- Better assert handling for exceptions in tests, e.g. at least type, but also the message.

- ValueOrDefault() method in command line parser results.
